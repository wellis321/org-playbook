# Database Configuration
# Copy this file to .env and update with your database credentials

DB_HOST=localhost
DB_PORT=8889
DB_NAME=org_playbook
DB_USER=root
DB_PASS=root
DB_CHARSET=utf8mb4

# Notes:
# - MAMP default MySQL port is 8889 (standard MySQL is 3306)
# - Check MAMP → Preferences → Ports for your MySQL port
# - Never commit the .env file to version control (it's already in .gitignore)
